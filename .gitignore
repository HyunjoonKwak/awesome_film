node_modules
.pnpm-store
.next
out
dist
build
coverage
.turbo
.tsbuildinfo
*.tsbuildinfo

# apps/desktop/build/ holds electron-builder asset inputs (icons, dmg
# background) — these are source files, not generated output.
!apps/desktop/build
!apps/desktop/build/**

.env
.env.local
.env.*.local

.DS_Store
*.log
.playwright-mcp/

# Whisper model — fetch via `pnpm --filter @cut/web prebundle:whisper`
apps/web/public/whisper/

# OpenCut clone used as study reference only
reference/
